# /home/MiguelAeTxio/PROJECTS/EnterpriseBot/ngrok.yml
version: "3"
agent:
  authtoken: ${NGROK_AUTHTOKEN}
  log: stdout
  log_level: info

endpoints:
  - name: enterprise_voice_bridge
    upstream:
      url: 8081
    inspect: true
