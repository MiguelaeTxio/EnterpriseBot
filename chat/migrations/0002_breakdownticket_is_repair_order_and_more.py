# Generated by Django 5.2.12 on 2026-05-18 10:32

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("chat", "0001_initial"),
        ("fleet", "0005_add_first_repair_to_machineasset"),
        ("ivr_config", "0023_contact_opt_out_broadcast_and_more"),
    ]

    operations = [
        migrations.AddField(
            model_name="breakdownticket",
            name="is_repair_order",
            field=models.BooleanField(
                default=False,
                help_text="True cuando el SUPERVISOR ha pulsado 'Convertir en orden de reparación' desde la vista de detalle del ticket.",
                verbose_name="Convertido en orden de reparación",
            ),
        ),
        migrations.AddField(
            model_name="breakdownticket",
            name="machine",
            field=models.ForeignKey(
                blank=True,
                help_text="MachineAsset resuelto por el agente Gemini a partir de machine_raw. Null hasta que el agente identifica la máquina en el catálogo.",
                null=True,
                on_delete=django.db.models.deletion.SET_NULL,
                related_name="breakdown_tickets",
                to="fleet.machineasset",
                verbose_name="Máquina / Centro de gasto",
            ),
        ),
        migrations.AddField(
            model_name="breakdownticket",
            name="photos",
            field=models.JSONField(
                default=list,
                help_text="Lista de rutas o URLs de las fotos adjuntas enviadas por el contacto durante el diálogo de recogida de avería. El agente Gemini acepta hasta 3 imágenes vía WhatsApp Media.",
                verbose_name="Fotos adjuntas",
            ),
        ),
        migrations.AddField(
            model_name="breakdownticket",
            name="section",
            field=models.ForeignKey(
                blank=True,
                help_text="Sección del contacto en el momento de abrir el ticket. Se registra para trazabilidad aunque la membresía M2M pueda cambiar.",
                null=True,
                on_delete=django.db.models.deletion.SET_NULL,
                related_name="breakdown_tickets",
                to="ivr_config.section",
                verbose_name="Sección",
            ),
        ),
        migrations.AddField(
            model_name="breakdownticket",
            name="ticket_number",
            field=models.PositiveIntegerField(
                blank=True,
                help_text="Número de ticket autoincremental por empresa. Se asigna automáticamente en el primer save() mediante MAX+1 dentro del scope de la empresa.",
                null=True,
                verbose_name="Número de ticket",
            ),
        ),
        migrations.AddField(
            model_name="chatroom",
            name="breakdown_contacts",
            field=models.ManyToManyField(
                blank=True,
                help_text="Contactos individuales adicionales con acceso directo a esta sala BREAKDOWNS, independientemente de su sección. Solo aplica a salas de tipo BREAKDOWNS.",
                related_name="breakdown_rooms",
                to="ivr_config.contact",
                verbose_name="Contactos individuales con acceso a Averías",
            ),
        ),
        migrations.AddField(
            model_name="chatroom",
            name="breakdown_sections",
            field=models.ManyToManyField(
                blank=True,
                help_text="Secciones cuyos miembros tienen acceso a esta sala BREAKDOWNS. Al añadir una sección, todos sus contactos quedan elegibles para enviar mensajes a la sala de averías. Solo aplica a salas de tipo BREAKDOWNS.",
                related_name="breakdown_rooms",
                to="ivr_config.section",
                verbose_name="Secciones con acceso a Averías",
            ),
        ),
    ]
