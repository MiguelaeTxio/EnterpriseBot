# /home/MiguelAeTxio/PROJECTS/EnterpriseBot/enterprise_core/settings.py
"""
Django settings for EnterpriseBot project.
Generated by EnterpriseBot Orchestrator - Refactored for Gemini 3.1 Pro Integration.
"""

import os
from pathlib import Path
from dotenv import load_dotenv

# 1. CARGA DE VARIABLES DE ENTORNO / LOAD ENVIRONMENT VARIABLES
BASE_DIR = Path(__file__).resolve().parent.parent
load_dotenv(BASE_DIR / '.env')

# 2. CONFIGURACIÓN DE SEGURIDAD / SECURITY CONFIGURATION
SECRET_KEY = os.getenv('SECRET_KEY')
DEBUG = os.getenv('DEBUG', 'False') == 'True'

# Inyección de Clave de API para Gemini 3.1 Pro / API Key Injection for Gemini
GEMINI_API_KEY = os.getenv('GEMINI_API_KEY')

# Lógica de lista para soportar múltiples hosts / Multiple hosts support
hosts_raw = os.getenv('ALLOWED_HOSTS', 'MiguelAeTxio.pythonanywhere.com')
ALLOWED_HOSTS = [h.strip() for h in hosts_raw.split(',') if h.strip()]

# 3. DEFINICIÓN DE APLICACIONES / APPLICATIONS DEFINITION
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'vox_bridge',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'enterprise_core.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'enterprise_core.wsgi.application'

# 4. CONFIGURACIÓN DE BASE DE DATOS / DATABASE CONFIGURATION
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': os.getenv('DB_NAME'),
        'USER': 'MiguelAeTxio',
        'PASSWORD': os.getenv('DB_PASSWORD'),
        'HOST': 'MiguelAeTxio.mysql.pythonanywhere-services.com',
        'PORT': '3306',
    }
}

AUTH_PASSWORD_VALIDATORS = [
    {'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator'},
    {'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator'},
    {'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator'},
    {'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator'},
]

# 5. INTERNACIONALIZACIÓN / INTERNATIONALIZATION
LANGUAGE_CODE = 'es-es'
TIME_ZONE = 'Europe/Madrid'
USE_I18N = True
USE_TZ = True

# 6. ARCHIVOS ESTÁTICOS / STATIC FILES
STATIC_URL = 'static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'static')

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
